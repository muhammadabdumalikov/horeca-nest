export enum UserRoles {
  SUPER_ADMIN = 1,
  ADMIN = 2,
  CLIENT = 3,
  DELIVER = 4,
  PROVIDER = 5
}

export enum UserRolesAsString {
  SUPER_ADMIN = '1',
  ADMIN = '2',
  CLIENT = '3',
  DELIVER = '4',
  PROVIDER = '5'
}

export enum UserStatus {
  REGISTERED = 0,
  ACTIVE = 1,
}

export enum PersonType {
  PHYSICAL = 1,
  JURIDIC = 2,
}
