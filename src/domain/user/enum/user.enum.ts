export enum UserRoles {
  SUPER_ADMIN = 1,
  ADMIN = 2,
  SELLER = 3,
  DELIVER = 4,
}

export enum UserStatus {
  REGISTERED = 0,
  ACTIVE = 1,
}

export enum PersonType {
  PHYSICAL = 1,
  JURIDIC = 2,
}
